namespace EasyFarm.UserSettings
{
    /// <summary>
    ///     Represents the command used to trigger the action.
    /// </summary>
    public enum PullFallbackType
    {
        None = 0,
        Lock = 1,
        Approach = 2,
    }
}